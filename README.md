# Naggenda Joshua — Full Stack Developer & Networking Profile

This folder contains a React (Vite) personal profile site with multi-route pages.

## Run locally
1. `npm install`
2. `npm run dev`

Then open the shown URL in your browser.

